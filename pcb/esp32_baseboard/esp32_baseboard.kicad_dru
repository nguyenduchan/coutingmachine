(version 1)

# Industrial-adjacent design floor (PCB_REVIEW A11–A12 / E6).
# JLCPCB house mins remain for hole/silk; copper-edge is 2.00 mm (not 0.30).

(rule "JLC min track"
	(constraint track_width (min 0.15mm))
)

(rule "JLC hole to hole"
	(constraint hole_to_hole (min 0.45mm))
)

(rule "JLC copper to hole"
	(constraint hole_clearance (min 0.20mm))
)

(rule "JLC courtyard"
	(constraint courtyard_clearance (min 0.30mm))
)

(rule "Copper to board edge 2mm"
	(constraint edge_clearance (min 2.00mm))
)

(rule "Via copper to component pad 1mm"
	(constraint clearance (min 1.00mm))
	(condition "A.Type == 'Via' && B.Type == 'Pad'")
)

(rule "JLC silk to pad"
	(constraint silk_clearance (min 0.15mm))
)
